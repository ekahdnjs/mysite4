<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="board">

	<resultMap type="com.mysite.vo.BoardVo" id="resultMapList">
	
			
		<result column="no" property="no"/>
 		<result column="title" property="title"/>
 		<result column="content" property="content"/>
 		<result column="hit" property="hit"/>
		<result column="reg_date" property="regDate" />
		<result column="user_No" property="userNo" />
		<result column="user_Name" property="userName" />	
		
		
	</resultMap>
	
	<select id="getList" resultMap = "resultMapList">
		
		<![CDATA[
	
					SELECT B.NO, TITLE, CONTENT, HIT, REG_DATE, USER_NO, u.NAME 
					FROM board b,USERS u
					where b.USER_NO = u.NO 
					order by no desc
		]]>
		
	</select>
	
	<insert id="insert" parameterType="com.mysite.vo.BoardVo">
		
		<![CDATA[
			insert into board
			values (seq_board_no.nextval, 
			#{title}, 
			#{content}, 
			#{hit},
			sysdate, 
			#{userNo} )
		]]>
	
	</insert>
	
	<delete id="delete" parameterType="com.mysite.vo.BoardVo">
		
		<![CDATA[
			delete from Board where no= #{no} and password= #{password}
		]]>
		
	</delete>
	
	
	
</mapper>